# ============================================================================
# Lab4All Web Application - Production Environment Configuration
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Fill in all the values marked with <REPLACE_*>
# 3. Keep this file secure - DO NOT commit .env.production to version control
# 4. Use strong, unique passwords for all services
#
# ============================================================================

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=<REPLACE_WITH_STRONG_RANDOM_STRING>
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"

# Application Settings
PORT=8000
LOG_LEVEL=info
WORKERS=4

# Database Configuration
DB_HOST=<REPLACE_WITH_DB_HOST>
# Examples:
#   - localhost (if database is on same server)
#   - postgres-db (if using Docker Compose)
#   - your-rds-endpoint.amazonaws.com (AWS RDS)
#   - your-db.postgres.database.azure.com (Azure)

DB_PORT=5432
DB_NAME=jal_sarovar_prod
DB_USER=<REPLACE_WITH_DB_USERNAME>
DB_PASSWORD=<REPLACE_WITH_DB_PASSWORD>

# Database Connection Pool Settings
DB_POOL_SIZE=10
DB_POOL_RECYCLE=3600
DB_MAX_OVERFLOW=20

# ML Models Configuration
ML_MODELS_PATH=/app/models
# In production, this should point to a persistent volume or S3 bucket

# Security Settings (Production Only)
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
PERMANENT_SESSION_LIFETIME=86400

# File Upload Settings
MAX_CONTENT_LENGTH=52428800
UPLOAD_FOLDER=/app/uploads
# In production, consider using cloud storage (S3, Azure Blob, GCS)

# SSL/TLS Configuration (Optional)
# SSL_KEY_FILE=/path/to/ssl/private.key
# SSL_CERT_FILE=/path/to/ssl/certificate.crt

# CORS Settings (if needed for API access)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Email Configuration (for alerts and notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=<REPLACE_WITH_EMAIL>
# MAIL_PASSWORD=<REPLACE_WITH_EMAIL_PASSWORD>
# MAIL_DEFAULT_SENDER=noreply@yourdomain.com

# Monitoring and Logging (Optional)
# SENTRY_DSN=<REPLACE_WITH_SENTRY_DSN>
# LOGSTASH_HOST=<REPLACE_WITH_LOGSTASH_HOST>
# LOGSTASH_PORT=5000

# Cloud Storage (Optional - for ML models and uploads)
# AWS_ACCESS_KEY_ID=<REPLACE_WITH_AWS_KEY>
# AWS_SECRET_ACCESS_KEY=<REPLACE_WITH_AWS_SECRET>
# AWS_REGION=ap-south-1
# S3_BUCKET=lab4all-uploads
# S3_MODELS_BUCKET=lab4all-ml-models

# Azure Configuration (Optional)
# AZURE_STORAGE_CONNECTION_STRING=<REPLACE_WITH_CONNECTION_STRING>
# AZURE_CONTAINER_NAME=lab4all-uploads

# Redis Configuration (Optional - for caching and sessions)
# REDIS_URL=redis://redis:6379/0
# CACHE_TYPE=redis
# CACHE_REDIS_URL=${REDIS_URL}

# Backup Configuration
# BACKUP_ENABLED=True
# BACKUP_S3_BUCKET=lab4all-backups
# BACKUP_RETENTION_DAYS=30
# BACKUP_SCHEDULE=0 2 * * *

# Application Monitoring
# ENABLE_METRICS=True
# METRICS_PORT=9090

# Rate Limiting (Optional)
# RATELIMIT_ENABLED=True
# RATELIMIT_DEFAULT=200 per day, 50 per hour

# Timezone
TZ=Asia/Kolkata

# ============================================================================
# IMPORTANT SECURITY NOTES:
# ============================================================================
# 1. Never use default passwords in production
# 2. Use environment-specific secret keys
# 3. Enable SSL/TLS for all production deployments
# 4. Regularly rotate database passwords and API keys
# 5. Use IAM roles instead of access keys when possible (AWS/Azure/GCP)
# 6. Enable database encryption at rest
# 7. Use VPC/Private networks for database connections
# 8. Enable database backups with point-in-time recovery
# 9. Monitor application logs for security incidents
# 10. Keep all dependencies updated with security patches
# ============================================================================
